# 交互与开发指南

## 0. 项目文档

- 开始任务前先阅读 [`docs/README.md`](docs/README.md)，再根据任务类型按需打开专题文档。
- 不要预先读取整个 `docs` 目录。只加载当前任务需要的文档。
- 文档与代码不一致时，以代码为准，并在本次改动中同步修正相关文档。

## 1. 语言规范

- **交互语言**：所有思考、回答与交流必须使用**中文**。
- **代码注释**：代码中的注释必须使用**中文**编写。注释应侧重于解释复杂的业务逻辑或"为什么要这样做"，而非简单的代码翻译。

## 2. 代码编写策略

### 2.1 文件管理（最小化新建）

- **优先修改现有文件**：在添加新功能时，首选在已有的相关文件中扩展代码。
- **慎重创建新文件/目录**：仅在现有文件过于庞大、职责不清，或新功能完全独立于现有模块时，才考虑新建文件或文件夹。

### 2.2 代码风格与复用

- 编写新逻辑前，先检查项目中是否存在可用的函数、组件、类型定义或工具类。
- 修改代码前，先检查相关模块的现有实现和调用方式，优先沿用项目已有模式。
- 严禁添加重复的辅助函数或类型定义，确保代码库精简与一致。

### 2.3 src 前端部分

- 禁止在 service 层外的地方直接调用 `invoke`
- 禁止在组件中直接使用 `useQuery、useQueryClient`

### 2.4 src-tauri 后端部分

- Rust 模块统一使用 <模块名>.rs + <模块名>/ 的组织方式，子模块声明写在同级的 <模块名>.rs ，不使用 mod.rs，不在模块声明文件中编写逻辑。

## 3. 代码质量与规范

### 3.1 工具链标准

- 本项目使用 **Biome** 进行代码格式化与 Lint 检查，使用 pnpm 包管理器（使用 pnpm 命令时出现 [WARN] Your node_modules are out of sync with your lockfile. The workspace structure has changed since last install 可以无视）。
- 本项目使用 i18next-cli 管理国际化。
- 涉及国际化字符串修改时，必须按照项目的 i18n Skill 完成翻译完整性检查。
- 完成代码修改和国际化字符串检查后，按需验证代码规范和执行类型检查：
  ```bash
  pnpm check # 该指令已经包含了代码格式化、Lint 检查和类型检查
  ```
- 如果涉及 rust 后端的修改，按需使用以下命令进行格式化和检查：
  ```bash
  cd src-tauri
  cargo fmt
  cargo clippy
  ```

### 3.2 命令执行规范

- 执行 Shell 命令时，**尽量一条一条执行**。
- **慎重**使用 `&&`、`;` 或 `|` 将多条命令合并在同一个调用中。确保每步操作的输出都能被独立捕获和分析。
